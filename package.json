{
  "name": "nextcloud-link",
  "version": "2.0.2",
  "description": "Javascript API to talk to Nextcloud",
  "type": "module",
  "typesVersions": {
    ">=4.9.5": {
      "*": [
        "./lib/types/client.d.ts"
      ]
    }
  },
  "main": "./lib/cjs/client.js",
  "module": "./lib/esm/client.js",
  "types": "./lib/types/client.d.ts",
  "browser": {
    "./lib/esm/client.js": "./bundle/nextcloud-client.es.min.js"
  },
  "exports": {
    "types": "./lib/types/client.d.ts",
    "node": "./lib/cjs/client.js",
    "require": "./lib/cjs/client.js",
    "import": "./lib/esm/client.js",
    "default": "./lib/esm/client.js"
  },
  "engines": {
    "node": ">=13.0.0"
  },
  "files": [
    "lib",
    "bundle"
  ],
  "scripts": {
    "build": "rimraf lib && rimraf bundle && npm run build:web && npm run build:node && npm run build:packageJson",
    "build:web": "rollup -c rollup.config.mjs",
    "build:node": "tsc --version && tsc -b ./tsconfig.cjs.json ./tsconfig.esm.json ./tsconfig.types.json",
    "build:packageJson": "echo '{\"type\": \"commonjs\"}' > lib/cjs/package.json && echo '{\"type\": \"module\"}' > lib/esm/package.json",
    "compile": "npx esbuild --bundle --platform=node --target=es2018 source/client.ts --outdir=compiled --minify",
    "types:generate": "npx typescript source/*.ts source/**/*.ts --declaration --emitDeclarationOnly --outDir compiled/source/",
    "docker": "npm run docker:stop && docker-compose -p nextcloud-link up -d",
    "docker:stop": "docker-compose -p nextcloud-link down -v",
    "lint": "tslint --project tsconfig.json",
    "await-nextcloud": "ts-node --project tsconfig.test.json ./tests/prepare-docker-nextcloud-container.ts",
    "test": "npm run docker && npm run await-nextcloud && NODE_OPTIONS=--experimental-vm-modules npx jest --config jest.conf.json --runInBand; npm run docker:stop",
    "test-ci": "npm run docker && npm run await-nextcloud && NODE_OPTIONS=--experimental-vm-modules npx jest --config jest.conf.json --runInBand",
    "test-watch": "npm run docker && npm run await-nextcloud && NODE_OPTIONS=--experimental-vm-modules npx jest --config jest.conf.json --runInBand --watch",
    "fast-test": "NODE_OPTIONS=--experimental-vm-modules NODE_OPTIONS=--experimental-vm-modules npx jest --config jest.conf.json --runInBand",
    "fast-test-watch": "npm run await-nextcloud && NODE_OPTIONS=--experimental-vm-modules npx jest --config jest.conf.json --runInBand --watch",
    "fast-test-watch-nostart": "npm run await-nextcloud && NODE_OPTIONS=--experimental-vm-modules npx jest --config jest.conf.json --runInBand --watch --testNamePattern=stopTestingAtStart"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tentwentyfour/nextcloud-link.git"
  },
  "keywords": [
    "nextcloud",
    "webdav"
  ],
  "author": "TenTwentyFour s.à r.l.",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/tentwentyfour/nextcloud-link/issues"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "lonad": "^0.3.0",
    "webdav": "^5.3.1"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^25.0.2",
    "@rollup/plugin-node-resolve": "^15.1.0",
    "@rollup/plugin-terser": "^0.4.3",
    "@rollup/plugin-typescript": "^11.1.2",
    "@types/jest": "^29.5.3",
    "@types/node": "^20.4.1",
    "esbuild": "^0.18.11",
    "eslint": "^8.44.0",
    "jest": "^29.6.1",
    "jest-jasmine2": "^29.6.1",
    "node-notifier": "^10.0.1",
    "rimraf": "^5.0.1",
    "rollup": "^3.26.2",
    "rollup-plugin-polyfill-node": "^0.12.0",
    "ts-jest": "^29.1.1",
    "ts-loader": "^9.4.4",
    "ts-node": "^10.9.1",
    "tslib": "^2.6.0",
    "typescript": "^4.9.4"
  }
}
