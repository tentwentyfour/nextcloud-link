{
  "name": "nextcloud.js",
  "version": "1.0.1",
  "description": "Javascript API to talk to Nextcloud",
  "main": "compiled/client.js",
  "scripts": {
    "test": "docker-compose down -v && docker-compose up -d && sleep 60 && jest --config jest.conf.js --runInBand; docker-compose down -v",
    "test-watch": "docker-compose down -v && docker-compose up -d && sleep 60 && jest --config jest.conf.js --runInBand --watch"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tentwentyfour/nextcloud.js.git"
  },
  "keywords": [
    "nextcloud"
  ],
  "author": "TenTwentyFour s.à r.l.",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/tentwentyfour/nextcloud.js/issues"
  },
  "dependencies": {
    "@types/node": "^9.6.4",
    "axios": "^0.18.0",
    "helpbox": "^7.1.0",
    "lodash.last": "^3.0.0",
    "webdav-client": "^1.4.0"
  },
  "types": "./compiled/source/client.d.ts",
  "devDependencies": {
    "@types/jest": "^22.2.2",
    "jest": "^22.4.3",
    "ts-jest": "^22.4.2",
    "ts-loader": "^3.3.1",
    "tslint": "^5.9.1",
    "tslint-config-airbnb": "^5.8.0",
    "tslint-loader": "^3.5.3",
    "typescript": "^2.8.1"
  }
}
